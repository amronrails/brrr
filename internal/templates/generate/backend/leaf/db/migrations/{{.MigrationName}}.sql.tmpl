-- +goose Up
CREATE TABLE {{.Table}} (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
{{- range .Columns }}
    {{.Name}} {{.SQLType}} NOT NULL{{if .Default}} DEFAULT {{.Default}}{{end}}{{if .Unique}} UNIQUE{{end}}{{if .IsRelation}} REFERENCES {{.RefTable}} (id) ON DELETE CASCADE{{end}},
{{- end }}
    created_at timestamptz NOT NULL DEFAULT now(),
    updated_at timestamptz NOT NULL DEFAULT now()
);

-- +goose Down
DROP TABLE IF EXISTS {{.Table}};
