// Package services holds the {{.Module}} module's business logic.
package services

import (
	"context"

	"github.com/google/uuid"

	"{{.ModulePath}}/internal/modules/{{.ModulePkg}}/internal/domain"
	"{{.ModulePath}}/internal/modules/{{.ModulePkg}}/internal/ports"
)

// {{.Pascal}}Service implements the {{.Snake}} use cases.
type {{.Pascal}}Service struct {
	repo ports.{{.Pascal}}Repository
}

// New{{.Pascal}}Service constructs a {{.Pascal}}Service.
func New{{.Pascal}}Service(repo ports.{{.Pascal}}Repository) *{{.Pascal}}Service {
	return &{{.Pascal}}Service{repo: repo}
}

func (s *{{.Pascal}}Service) Create(ctx context.Context, in domain.Create{{.Pascal}}Input) (domain.{{.Pascal}}, error) {
	return s.repo.Create(ctx, in)
}

func (s *{{.Pascal}}Service) Get(ctx context.Context, id uuid.UUID) (domain.{{.Pascal}}, error) {
	return s.repo.Get(ctx, id)
}

// List returns a page of {{.Plural}} along with the total count.
func (s *{{.Pascal}}Service) List(ctx context.Context, limit, offset int32) ([]domain.{{.Pascal}}, int64, error) {
	if limit <= 0 || limit > 100 {
		limit = 20
	}
	if offset < 0 {
		offset = 0
	}
	items, err := s.repo.List(ctx, limit, offset)
	if err != nil {
		return nil, 0, err
	}
	total, err := s.repo.Count(ctx)
	if err != nil {
		return nil, 0, err
	}
	return items, total, nil
}

func (s *{{.Pascal}}Service) Update(ctx context.Context, id uuid.UUID, in domain.Update{{.Pascal}}Input) (domain.{{.Pascal}}, error) {
	return s.repo.Update(ctx, id, in)
}

func (s *{{.Pascal}}Service) Delete(ctx context.Context, id uuid.UUID) error {
	return s.repo.Delete(ctx, id)
}
